{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build, Test, Doc",
            "type": "shell",
            "command": "nix-shell --pure --run 'cargo build && cargo test && cargo doc'",
            "problemMatcher": [ ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
